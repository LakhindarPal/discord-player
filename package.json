{
  "name": "discord-player",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "author": "Androz2091",
  "scripts": {
    "bootstrap": "cd ./scripts && node bootstrap.js",
    "build": "turbo run build",
    "prepublish": "lerna run prepublish",
    "format": "prettier --write \"{packages,scripts}/**/*.{js,ts}\""
  },
  "devDependencies": {
    "ansi-colors": "^4.1.3",
    "common-tags": "^1.8.2",
    "fs-extra": "^11.1.0",
    "lerna": "^6.4.0",
    "prettier": "^2.8.2",
    "turbo": "^1.6.3"
  }
}
